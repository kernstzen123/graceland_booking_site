'use client';

import { useEffect, useState } from 'react';
import { supabaseBrowser } from '@/lib/supabase-browser';

type Redemption = { id: string; booking_id: string; amount_used: number; created_at: string; bookings?: { reference: string; visit_date: string } | Array<{ reference: string; visit_date: string }> };
type Voucher = { id: string; credit_code: string; original_amount: number; remaining_balance: number; status: string; created_at: string; issued_by?: string; bookings?: { reference: string; customers?: { first_name: string; last_name: string; email: string } | Array<{ first_name: string; last_name: string; email: string }> } | Array<{ reference: string; customers?: { first_name: string; last_name: string; email: string } | Array<{ first_name: string; last_name: string; email: string }> }>; credit_redemptions?: Redemption[] };
type Summary = { totalIssued: number; totalRedeemed: number; outstanding: number; totalVoided: number };
type Failure = { id: string; recipient: string | null; error_message: string; created_at: string };
const first = <T,>(value: T | T[] | undefined) => Array.isArray(value) ? value[0] : value;

export default function VouchersAdmin() {
  const [vouchers, setVouchers] = useState<Voucher[]>([]); const [summary, setSummary] = useState<Summary | null>(null); const [failures, setFailures] = useState<Failure[]>([]); const [query, setQuery] = useState(''); const [status, setStatus] = useState(''); const [message, setMessage] = useState('Loading vouchers...'); const [busy, setBusy] = useState(''); const [expanded, setExpanded] = useState<string | null>(null);
  const token = async () => (await supabaseBrowser.auth.getSession()).data.session?.access_token || '';
  const load = async () => { const headers = { Authorization: `Bearer ${await token()}` }; const [listResponse, summaryResponse, failuresResponse] = await Promise.all([fetch(`/api/admin/vouchers?q=${encodeURIComponent(query)}&status=${status}`, { headers, cache: 'no-store' }), fetch('/api/admin/vouchers/summary', { headers, cache: 'no-store' }), fetch('/api/admin/notifications', { headers, cache: 'no-store' })]); const list = await listResponse.json(); const totals = await summaryResponse.json(); const failed = await failuresResponse.json(); if (!listResponse.ok) throw new Error(list.error); setVouchers(list.vouchers); setSummary(totals.summary); setFailures(failed.failures || []); setMessage(list.vouchers.length ? '' : 'No vouchers found.'); };
  useEffect(() => { load().catch(error => setMessage(error.message)); }, [status]);
  const retry = async (failureId: string) => { if (busy) return; setBusy('retry'); setMessage('Retrying email... please wait.'); try { const response = await fetch('/api/admin/notifications', { method: 'POST', headers: { Authorization: `Bearer ${await token()}`, 'Content-Type': 'application/json' }, body: JSON.stringify({ failureId }) }); const data = await response.json(); setMessage(data.message || data.error); if (response.ok) await load(); } finally { setBusy(''); } };
  const voidVoucher = async (voucher: Voucher) => { if (busy) return; const reason = window.prompt('Reason for voiding this voucher:'); if (!reason?.trim() || !window.confirm(`Permanently void ${voucher.credit_code}? Its remaining balance will be retained for audit purposes.`)) return; setBusy('void'); setMessage('Voiding voucher... please wait.'); try { const response = await fetch(`/api/admin/vouchers/${voucher.id}/void`, { method: 'POST', headers: { Authorization: `Bearer ${await token()}`, 'Content-Type': 'application/json' }, body: JSON.stringify({ reason }) }); const data = await response.json(); setMessage(data.message || data.error); if (response.ok) await load(); } finally { setBusy(''); } };
  return <main className="container" style={{ padding: '2rem 1rem' }}><div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', gap: 12, flexWrap: 'wrap', marginBottom: '1.5rem' }}><div><p style={{ color: 'var(--primary)', fontWeight: 700 }}>FINANCE OPERATIONS</p><h1>Rebooking vouchers</h1></div><a className="btn" href="/admin/bookings">All bookings</a></div>{summary && <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit,minmax(170px,1fr))', gap: 12, marginBottom: '1rem' }}>{[['Outstanding', summary.outstanding], ['Total issued', summary.totalIssued], ['Total redeemed', summary.totalRedeemed], ['Total voided', summary.totalVoided]].map(([label, value]) => <div className="card" key={String(label)}><p style={{ color: 'var(--text-muted)' }}>{label}</p><strong style={{ fontSize: '1.6rem', color: label === 'Outstanding' ? 'var(--warning)' : 'var(--primary)' }}>R {Number(value).toFixed(2)}</strong></div>)}</div>}<div className="card" style={{ marginBottom: '1rem' }}><form onSubmit={event => { event.preventDefault(); load().catch(error => setMessage(error.message)); }} style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}><input value={query} onChange={event => setQuery(event.target.value)} placeholder="Search code, customer, email, or booking" style={{ flex: 1, padding: 10 }} /><select value={status} onChange={event => setStatus(event.target.value)} style={{ padding: 10 }}><option value="">All statuses</option><option value="active">Active</option><option value="depleted">Depleted</option><option value="void">Void</option></select><button className="btn btn-primary">Search</button></form></div>{failures.length > 0 && <div className="card" style={{ marginBottom: '1rem', border: '1px solid var(--danger)' }}><h2>Failed voucher emails</h2>{failures.map(failure => <div key={failure.id} style={{ display: 'flex', justifyContent: 'space-between', gap: 12, flexWrap: 'wrap', borderTop: '1px solid var(--border-color)', padding: '0.75rem 0' }}><span>{failure.recipient || 'Unknown recipient'}<br /><small style={{ color: 'var(--danger)' }}>{failure.error_message}</small></span><button className="btn" onClick={() => retry(failure.id)}>Retry email</button></div>)}</div>}{message && <p style={{ color: 'var(--text-muted)', marginBottom: '1rem' }}>{message}</p>}<div className="card" style={{ overflowX: 'auto' }}><table style={{ width: '100%', borderCollapse: 'collapse', minWidth: 1050 }}><thead><tr>{['Customer', 'Code', 'Original', 'Remaining', 'Status', 'Issued', 'Issued by', 'Last used', 'Action'].map(header => <th key={header} style={{ textAlign: 'left', padding: 10, borderBottom: '2px solid var(--border-color)' }}>{header}</th>)}</tr></thead><tbody>{vouchers.map(voucher => { const booking = first(voucher.bookings); const customer = booking && first(booking.customers); const redemptions = voucher.credit_redemptions || []; const lastUsed = redemptions.length ? redemptions.reduce((latest, item) => item.created_at > latest ? item.created_at : latest, redemptions[0].created_at) : null; return <tr key={voucher.id}><td style={{ padding: 10 }}>{customer ? `${customer.first_name} ${customer.last_name}` : 'Customer'}<br /><small>{customer?.email}</small></td><td style={{ padding: 10, fontWeight: 700 }}>{voucher.credit_code}</td><td style={{ padding: 10 }}>R {Number(voucher.original_amount).toFixed(2)}</td><td style={{ padding: 10 }}>R {Number(voucher.remaining_balance).toFixed(2)}</td><td style={{ padding: 10 }}>{voucher.status}</td><td style={{ padding: 10 }}>{new Date(voucher.created_at).toLocaleDateString()}</td><td style={{ padding: 10 }}>{voucher.issued_by || 'Bulk refund'}</td><td style={{ padding: 10 }}>{lastUsed ? new Date(lastUsed).toLocaleDateString() : 'Never'}</td><td style={{ padding: 10 }}><button className="btn" onClick={() => setExpanded(expanded === voucher.id ? null : voucher.id)}>{expanded === voucher.id ? 'Hide history' : 'History'}</button>{voucher.status === 'active' && <button className="btn" onClick={() => voidVoucher(voucher)} style={{ marginLeft: 6, color: 'var(--danger)', border: '1px solid var(--danger)' }}>Void</button>}{expanded === voucher.id && <div style={{ marginTop: 10, minWidth: 260 }}>{redemptions.length ? redemptions.map(redemption => { const usedBooking = first(redemption.bookings); return <p key={redemption.id} style={{ borderTop: '1px solid var(--border-color)', padding: '6px 0', fontSize: 13 }}>{usedBooking?.reference || redemption.booking_id} · R {Number(redemption.amount_used).toFixed(2)} · {new Date(redemption.created_at).toLocaleString()}</p>; }) : <p style={{ color: 'var(--text-muted)' }}>No redemptions.</p>}</div>}</td></tr>; })}</tbody></table></div></main>;
}
